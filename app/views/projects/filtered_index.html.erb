<h1> Projects for <%=@city%></h1>

<%=link_to("Sign Out", destroy_user_session_path, :method => "delete") %>
<ul>
<% @projects.each do |project| %>
  <li><%=link_to(project.name, "/projects/#{project.id}") %> : Created at: <%= project.created_at%> </li>
<% end %>