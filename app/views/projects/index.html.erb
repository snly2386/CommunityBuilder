<%=link_to("Sign Out", destroy_user_session_path, :method => "delete") %>

<h1> Select a City </h1>
<ul>
<% @cities.each do |city| %>
<li><%=link_to(city, filtered_index_path(:city => city)) %></li>
<% end %>
</ul>

